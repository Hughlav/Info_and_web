To build the project simply run "make build" from info_and_web directory

To index and run the batch queries simply run "make run"

To run the evaluations with trec eval:
    cd into the trec folder "cd trec_eval.8.1"
    and run "./trec_eval QRelsCorrectedforTRECeval results.txt"
